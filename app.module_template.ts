import { ReactiveFormsModule, FormsModule } from '@angular/forms';
[...]
  imports: [
    BrowserModule,
    AppRoutingModule,
    HttpClientModule,
    ReactiveFormsModule,
    FormsModule
  ],
[...]